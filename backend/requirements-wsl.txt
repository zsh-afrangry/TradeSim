# TradeSim WSL/Linux 环境依赖
# 由 Windows conda 环境 tradesim 导出，已移除 Windows 专属包
# 安装命令: pip install -r requirements-wsl.txt

# ── 核心框架 ──────────────────────────────────────────────
fastapi==0.129.0
uvicorn==0.41.0
starlette==0.52.1
pydantic==2.12.5
pydantic_core==2.41.5
python-dotenv==1.2.1

# ── 数据处理 ──────────────────────────────────────────────
polars==1.38.1
pandas==2.3.3
numpy==2.2.6
pyarrow==23.0.1

# ── 股票数据 ──────────────────────────────────────────────
akshare==1.18.25

# ── 数据库 ────────────────────────────────────────────────
SQLAlchemy==2.0.46
pymongo==4.16.0
motor==3.7.1
PyMySQL==1.1.2
dnspython==2.8.0

# ── AI / HTTP ─────────────────────────────────────────────
openai==2.21.0
httpx==0.28.1
httpcore==1.0.9
requests==2.32.5
curl_cffi==0.14.0
h11==0.16.0

# ── 解析工具 ──────────────────────────────────────────────
beautifulsoup4==4.14.3
html5lib==1.1
lxml==6.0.2
soupsieve==2.8.3
webencodings==0.5.1
et_xmlfile==2.0.0
openpyxl==3.1.5
xlrd==2.0.2

# ── 工具库 ────────────────────────────────────────────────
anyio==4.12.1
click==8.3.1
colorama==0.4.6
cryptography==46.0.0
decorator==5.2.1
exceptiongroup==1.3.1
greenlet==3.3.1
idna==3.11
jiter==0.13.0
jsonpath==0.82.2
packaging==25.0
python-dateutil==2.9.0.post0
pytz==2025.2
six==1.17.0
sniffio==1.3.1
tabulate==0.9.0
tqdm==4.67.3
typing_extensions==4.15.0
typing-inspection==0.4.2
tzdata==2025.3
urllib3==2.6.3
annotated-types==0.7.0
certifi==2026.1.4
charset-normalizer==3.4.4
cffi==2.0.0

# ── 以下为 Windows 专属，WSL 中已移除 ─────────────────────
# pywin32-ctypes  (Windows only)
# pyinstaller     (Windows only，打包工具)
# pyinstaller-hooks-contrib
# pefile          (Windows PE格式，Windows only)
# altgraph        (PyInstaller 依赖，不需要)
# mini-racer      (可选，某些 akshare 功能需要，Linux 可能需手动编译)
